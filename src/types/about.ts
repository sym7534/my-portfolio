/** Icon keys usable in about/hero inline text. The actual image map lives in
 * components/InlineIcon.tsx; the type lives here so the data layer never
 * imports from components. */
export type AboutIconKey =
  | "uwaterloo"
  | "canada"
  | "minecraft"
  | "balatro"
  | "solidworks"
  | "claude"
  | "neuralink"
  | "ib";
